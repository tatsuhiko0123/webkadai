<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  </head>
  <body>
    <h1>テスト！！！！！！！！！！！！！</h1>
    <% require 'pg' %>
    <% connection = PG::connect(dbname: "goya") %>
    <% connection.internal_encoding = "UTF-8" %>
    <form action="indicate.cgi" method="POST">
       下記に文字を入力してください<br><br>
      <input type="text" name="input" ><br>
      <input type="submit" name="送信" >
    </form>
    <% result = connection.exec("select weight, give_for from crops;") %>
    <% data = [] %>
    <% result.each do |record| %>
      <%  data << "ゴーヤの大きさ：#{record["weight"]}　売った相手：#{record["give_for"]}" %>
    <% end %>
    <form action="goya.cgi" method="POST">
      下記のボタンを押すと、ゴーヤの大きさと売った相手の情報ページに飛びます<br><br>
      <!-- valueには、送りたいデータを代入する -->
      <!-- name="goya"を、情報の目印とする -->
      <input type="text" name="goya" value="<%= data.join(' ') %>">
      <input type="submit" name="送信" >
    </form>

    <% connection.exec("select give_for from crops where
                                 not(give_for = '自家消費');") %>
    <% data2 = "出荷先：#{}" %>
    <form action="goya.cgi" method="POST">
       下記のボタンを押すと、出荷先の情報ページに飛びます<br><br>
       <!-- valueには、送りたいデータを代入する -->
       <!-- name="goya"を、情報の目印とする -->
      <input type="text" name="goya" value="<%= data2 %>">
      <input type="submit" name="送信" >
    </form>

    <% connection.exec("select quality from crops where
                              not(quality = true);") %>
    <% data3 = "ゴーヤの品質：#{"quality"}" %>
    <form action="goya.cgi" method="POST">
    下記のボタンを押すと、ゴーヤの品質の情報ページに飛びます<br><br>
    <!-- valueには、送りたいデータを代入する -->
    <!-- name="goya"を、情報の目印とする -->
   <input type="text" name="goya" value="<%= data3 %>">
   <input type="submit" name="送信" >
 </form>
  </body>
</html>


<% data1 = connection.exec("select give_for from crops
                                not(give_for = "実家消費");") %>
    <form action="goya.cgi" method="POST">
       下記のボタンを押すと、ゴーヤの出荷場所の情報ページに飛びます<br><br>
       <!-- valueには、送りたいデータを代入する -->
       <!-- name="goya"を、情報の目印とする -->
      <input type="text" name="goya_give" value="<%= data1.join(' ') %>">
      <input type="submit" name="送信" >
    </form>
    <% data2 = connection.exec("select quality from crops
                                quality = false;") %>
    <form action="goya.cgi" method="POST">
       下記のボタンを押すと、ゴーヤの出荷場所の情報ページに飛びます<br><br>
       <!-- valueには、送りたいデータを代入する -->
       <!-- name="goya"を、情報の目印とする -->
      <input type="text" name="goya_quality" value="<%= data2.join(' ') %>">
      <input type="submit" name="送信" >
    </form>
